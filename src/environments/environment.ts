/**
 * Default environment (used when no config is specified).
 * In build/serve, this file is replaced by the target env via fileReplacements.
 */
export const environment = {
  production: false,
  name: 'default',
  apiUrl: ''
};
